# YAML Schema — `ResourceType`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `ResourceType.cs`

- **YAML tag**: `!resource`
- **manifest.yaml section**: `resource:`
- **Inherits**: `StaticDataCollectionItem<ResourceType>, IIndexable`

## Template (every serializable field)

```yaml
my_resource_key:
  isPlaceholder: ~        # bool   # ⚠ never used in official YAML
  name: ~        # string
  prefabName: ~        # string
  iconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  altIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  orbitalIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  altOrbitalIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  veinIconsName: ~        # YAML list; sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  color: ~        # hex color (e.g. 8B4513) or {r,g,b,a} map
  knowledge: ~        # !knowledge <key> reference
  materialType: ~        # enum MaterialType (Manufactured, Mined, Released, Placement, Placeholder)
  cubeMaterialName: ~        # string   # ⚠ never used in official YAML
  showInScannerLens: ~        # bool
  radius: ~        # float
  showAsInput: ~        # bool
  disableWhenFlooded: ~        # bool
  quantityStatName: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  potentialDemandStatName: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  currentDemandStatName: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  potentialProductionStatName: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  currentProductionStatName: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  index: ~        # int
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `isPlaceholder` | `bool` | ✅ YamlMember (confirmed) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `name` | `string` | ✅ YamlMember (confirmed) | 42× | string |
| `prefabName` | `string` | ✅ YamlMember (confirmed) | 29× | string |
| `iconName` | `Sprite` | ✅ YamlMember (confirmed) | 37× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `altIconName` | `Sprite` | ✅ YamlMember (confirmed) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `orbitalIconName` | `Sprite` | ✅ YamlMember (confirmed) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `altOrbitalIconName` | `Sprite` | ✅ YamlMember (confirmed) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `veinIconsName` | `List<Sprite>` | ✅ YamlMember (confirmed) | 11× | YAML list; sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `color` | `Color` | ✅ YamlMember (confirmed) | 29× | hex color (e.g. 8B4513) or {r,g,b,a} map |
| `knowledge` | `KnowledgeType` | ✅ YamlMember (confirmed) | 29× | !knowledge <key> reference |
| `materialType` | `MaterialType` | ✅ YamlMember (confirmed) | 42× | enum MaterialType (Manufactured, Mined, Released, Placement, Placeholder) |
| `cubeMaterialName` | `string` | ✅ YamlMember (confirmed) | 0 ⚠ | string |
| `showInScannerLens` | `bool` | ✅ YamlMember (confirmed) | 2× | bool |
| `radius` | `float` | ✅ YamlMember (confirmed) | 1× | float |
| `showAsInput` | `bool` | ✅ YamlMember (confirmed) | 1× | bool |
| `disableWhenFlooded` | `bool` | ✅ YamlMember (confirmed) | 9× | bool |
| `quantityStatName` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 0 ⚠ | string |
| `potentialDemandStatName` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 0 ⚠ | string |
| `currentDemandStatName` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 0 ⚠ | string |
| `potentialProductionStatName` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 0 ⚠ | string |
| `currentProductionStatName` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 0 ⚠ | string |
| `index` | `int` | ✅ YamlMember (confirmed) | 27× | int |
| `table` | `Dictionary<string, T>` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `StaticValues` | `Dictionary<string, T>.ValueCollection` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `key` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `rootPath` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `resolved` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `isMock` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `veinIconAtlas` | `Texture2D` | ⚙️ runtime only (not serialized) | 0 ⚠ | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `_MaxIndex` | `int` | ⚙️ runtime only (not serialized) | 0 ⚠ | int |
| `NonVirtualResources` | `ResourceType[]` | ⚙️ runtime only (not serialized) | 0 ⚠ | YAML list; !resource <key> reference |
| `ValueByIndex` | `ResourceType[]` | ⚙️ runtime only (not serialized) | 0 ⚠ | YAML list; !resource <key> reference |

## ⚠ Fields present in official YAML but absent from current C#

Either renamed via `YamlMember(Name=…)` (name string lives in the binary, not readable from stubs — e.g. `cubeMaterialName` → `cubeMaterial`), or **obsolete fields silently ignored by the loader** (the vanilla files themselves carry dead fields).

- `cubeMaterial` (14×)

## Enum `MaterialType`

- `Manufactured`
- `Mined`
- `Released`
- `Placement`
- `Placeholder`

## Enum `UnitType`

- `Kilograms`
- `Liters`
- `CubicMeters`

## Constants (known vanilla keys)

- `WATER` = `resource_water`
- `SILICON` = `resource_silicon`
- `ALUMINUM` = `resource_aluminum`
- `IRON` = `resource_iron`
- `CARBON` = `resource_carbon`
- `STEEL` = `resource_steel`
- `GLASS` = `resource_glass`
- `ELECTRONICS` = `resource_electronics`
- `FOOD` = `resource_food`
- `PARTS` = `resource_parts`
- `CHEMICALS` = `resource_chemicals`
- `POLYMERS` = `resource_polymers`
- `DRONE` = `resource_drone`
- `SHIP` = `resource_ship`
- `RESEARCH_POINTS` = `resource_research_points`
- `MILITARY_DRONE` = `resource_military_drone`
- `REPAIR_DRONE` = `resource_repair_drone`
- `OXYGEN_RELEASE` = `resource_oxygen_release`
- `OXYGEN_CAPTURE` = `resource_oxygen_capture`
- `OXYGEN_RESPIRATION` = `resource_oxygen_respiration`
- `NITROGEN_RELEASE` = `resource_nitrogen_release`
- `CARBON_DIOXIDE_RELEASE` = `resource_carbon_dioxide_release`
- `GHG_RELEASE` = `resource_ghg_release`
- `NITRATE` = `resource_nitrate`
- `HEAT` = `resource_heat`
- `HABITABLE_CRATER` = `resource_crater`
- `SPECIAL_SITE` = `resource_special_site`
- `LAND_AREA` = `resource_land_area`
- `TREES` = `resource_trees`
- `O2_UP` = `resource_O2_Up`
- `CO2_DOWN` = `resource_CO2_Down`
- `RESEARCH_BONUS` = `resource_research_bonus`
- `LICHEN` = `resource_lichen`
- `CYANOBACTERIA` = `resource_water_with_cyanobacteria`
- `SEA_WATER` = `resource_sea_water`
- `WATER_FLOW` = `resource_water_flow`
- `WATER_AREA` = `resource_water_area`
- `HUMIDITY` = `resource_humidity`

## Referenced types

- [`KnowledgeType`](KnowledgeType.md)
- [`MaterialType`](MaterialType.md)
