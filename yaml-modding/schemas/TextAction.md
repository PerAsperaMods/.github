# YAML Schema — `TextAction`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `TextAction.cs`


## Template (every serializable field)

```yaml
my_textaction_key:
  daysDelay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
  command: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  arguments: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
  showInFrontend: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `daysDelay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `command` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `arguments` | `List<string>` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `showInFrontend` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
