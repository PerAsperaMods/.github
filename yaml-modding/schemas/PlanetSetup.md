# YAML Schema — `PlanetSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `PlanetSetup.cs`

- **Inherits**: `YAMLSetup<PlanetSetup>`

## Template (every serializable field)

```yaml
radiationFiresPerYear: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
flammabilityFiresPerYear: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
minOxygenForCombustion: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterDmgPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
flammabilityLowOxygenRatio: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
flammabilityHighOxygenRatio: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxSlopeForEvenSurface: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
equatorialStripDistance: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxResourceVeinAmount: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
co2FreezingTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterFreezingTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
conversionCO2ToO2: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
axialTilt: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
blackBodyTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
blackBodyTemperatureAdditional: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialMinTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialAverageTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialMaxTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
albedo: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
daysPerYear: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
daysPerMonth: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
monthsPerYear: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
ghgTemperatureIncreaseFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
ghgTemperatureIncreasePressureModifier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
co2TemperatureIncreaseFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
co2TemperatureIncreasePressureModifier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
o3TemperatureIncreaseFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
o3TemperatureIncreasePressureModifier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
co2IceCapPressureSystemFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
co2IceCapPressureSystemInverseTemperatureMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
regolithSystemDepositsMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
regolithSystemTemperatureMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
regolithSystemDepositsExponent: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
poleTemperatureMainFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
poleTemperatureCO2PressureMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
poleTemperatureCO2PressureAdditionalTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
iceToWaterConversionRatio: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
dailyIceMeltTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
baseTemperature: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxTemperatureFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxTemperatureSinPhiFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxTemperatureSinPhiSquaredFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
minTemperatureFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
temperatureAltitudeFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
temperatureVarianceExponent: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
solarPowerFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
eolicPowerFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
abHadleyCellLimitMainTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
abHadleyCellLimitSinPhiTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
abHadleyCellLimitSinPhiSquaredTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
bcHadleyCellLimitMainTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
bcHadleyCellLimitSinPhiTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
bcHadleyCellLimitSinPhiSquaredTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cdHadleyCellLimitMainTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cdHadleyCellLimitSinPhiTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cdHadleyCellLimitSinPhiSquaredTerm: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humidityNeighbourWeight: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humidityOwnWeight: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialO2Pressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialN2Pressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialCO2Pressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialArgonPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialH2OPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialGHGPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialCO2FrozenDeposits: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialCO2RegolithDeposits: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
initialWaterPermafrostDeposits: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxRegolithAbsorption: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
maxWaterPermafrostDeposits: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
permafrostWaterDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
frozenCO2DailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
blackBodyTemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
co2TemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
o3TemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
ghgTemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
spaceMirrorTemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cometTemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
deimosTemperatureDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterStockDailyDeltaFactorPositive: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterStockDailyDeltaFactorNegative: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
co2RegolithDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humanMinimumPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humanMaximumPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
advancedPlantOxygenPressureGoal: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humanMinimumOxygenPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humanMaximumOxygenPressure: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
humanCO2Toxicity: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
ghgHalfLifeInYears: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
polarDustHalfLifeInYears: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
polarNukeHalfLifeInYears: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cometAdditionalTemperatureHalfLifeInYears: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
deimosAdditionalTemperatureHalfLifeInYears: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
excessAtmosphereHalfLifeInYears: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
spaceMirrorTotalTemperatureIncrease: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterLevelTable_stock: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
waterLevelTable_level: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
waterLevelTable_area: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
solarLongitudeTable_sol: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
solarLongitudeTable_lng: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
rivalExclusionRadius: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
polarExclusionRadius: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
farmMinHumidity: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
farmMaxMetal: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
farmMaxCliff: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaMaxGlobalGenerationPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenMaxGlobalGenerationPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsMaxGlobalGenerationPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenO2PressureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenO2PressureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenCO2PressureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenCO2PressureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenKillTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenKillTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenGrowthTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenGrowthTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenHumidityLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenHumidityHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenUpperKillTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenUpperKillTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenUpperGrowthTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
lichenUpperGrowthTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsO2PressureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsO2PressureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsCO2PressureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsCO2PressureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsKillTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsKillTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsGrowthTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsGrowthTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsHumidityLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsHumidityHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsUpperKillTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsUpperKillTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsUpperGrowthTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
plantsUpperGrowthTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaO2PressureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaO2PressureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaCO2PressureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaCO2PressureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaKillTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaKillTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaGrowthTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaGrowthTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaUpperKillTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaUpperKillTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaUpperGrowthTemperatureLowEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
cyanobacteriaUpperGrowthTemperatureHighEdge: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
faunaDailyDeltaFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
faunaCO2Toxicity: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
riversEnabled: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `radiationFiresPerYear` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `flammabilityFiresPerYear` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `minOxygenForCombustion` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterDmgPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `flammabilityLowOxygenRatio` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `flammabilityHighOxygenRatio` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxSlopeForEvenSurface` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `equatorialStripDistance` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxResourceVeinAmount` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `co2FreezingTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterFreezingTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `conversionCO2ToO2` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `axialTilt` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `blackBodyTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `blackBodyTemperatureAdditional` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialMinTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialAverageTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialMaxTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `albedo` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `daysPerYear` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `daysPerMonth` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `monthsPerYear` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `ghgTemperatureIncreaseFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `ghgTemperatureIncreasePressureModifier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `co2TemperatureIncreaseFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `co2TemperatureIncreasePressureModifier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `o3TemperatureIncreaseFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `o3TemperatureIncreasePressureModifier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `co2IceCapPressureSystemFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `co2IceCapPressureSystemInverseTemperatureMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `regolithSystemDepositsMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `regolithSystemTemperatureMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `regolithSystemDepositsExponent` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `poleTemperatureMainFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `poleTemperatureCO2PressureMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `poleTemperatureCO2PressureAdditionalTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `iceToWaterConversionRatio` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `dailyIceMeltTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `baseTemperature` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxTemperatureFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxTemperatureSinPhiFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxTemperatureSinPhiSquaredFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `minTemperatureFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `temperatureAltitudeFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `temperatureVarianceExponent` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `solarPowerFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `eolicPowerFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `abHadleyCellLimitMainTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `abHadleyCellLimitSinPhiTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `abHadleyCellLimitSinPhiSquaredTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `bcHadleyCellLimitMainTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `bcHadleyCellLimitSinPhiTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `bcHadleyCellLimitSinPhiSquaredTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cdHadleyCellLimitMainTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cdHadleyCellLimitSinPhiTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cdHadleyCellLimitSinPhiSquaredTerm` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humidityNeighbourWeight` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humidityOwnWeight` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialO2Pressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialN2Pressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialCO2Pressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialArgonPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialH2OPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialGHGPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialCO2FrozenDeposits` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialCO2RegolithDeposits` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `initialWaterPermafrostDeposits` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxRegolithAbsorption` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `maxWaterPermafrostDeposits` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `permafrostWaterDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `frozenCO2DailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `blackBodyTemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `co2TemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `o3TemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `ghgTemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `spaceMirrorTemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cometTemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `deimosTemperatureDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterStockDailyDeltaFactorPositive` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterStockDailyDeltaFactorNegative` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `co2RegolithDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humanMinimumPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humanMaximumPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `advancedPlantOxygenPressureGoal` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humanMinimumOxygenPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humanMaximumOxygenPressure` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `humanCO2Toxicity` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `ghgHalfLifeInYears` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `polarDustHalfLifeInYears` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `polarNukeHalfLifeInYears` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cometAdditionalTemperatureHalfLifeInYears` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `deimosAdditionalTemperatureHalfLifeInYears` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `excessAtmosphereHalfLifeInYears` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `spaceMirrorTotalTemperatureIncrease` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterLevelTable_stock` | `float[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `waterLevelTable_level` | `float[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `waterLevelTable_area` | `float[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `solarLongitudeTable_sol` | `float[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `solarLongitudeTable_lng` | `float[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `rivalExclusionRadius` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `polarExclusionRadius` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `farmMinHumidity` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `farmMaxMetal` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `farmMaxCliff` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaMaxGlobalGenerationPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenMaxGlobalGenerationPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsMaxGlobalGenerationPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenO2PressureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenO2PressureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenCO2PressureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenCO2PressureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenKillTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenKillTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenGrowthTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenGrowthTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenHumidityLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenHumidityHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenUpperKillTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenUpperKillTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenUpperGrowthTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lichenUpperGrowthTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsO2PressureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsO2PressureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsCO2PressureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsCO2PressureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsKillTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsKillTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsGrowthTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsGrowthTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsHumidityLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsHumidityHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsUpperKillTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsUpperKillTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsUpperGrowthTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `plantsUpperGrowthTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaO2PressureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaO2PressureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaCO2PressureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaCO2PressureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaKillTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaKillTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaGrowthTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaGrowthTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaUpperKillTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaUpperKillTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaUpperGrowthTemperatureLowEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `cyanobacteriaUpperGrowthTemperatureHighEdge` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `faunaDailyDeltaFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `faunaCO2Toxicity` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `riversEnabled` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |
