# YAML Schema — `TextureOverrideData`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `ModelPipeline.cs`


## Template (every serializable field)

```yaml
my_textureoverridedata_key:
  materialIndex: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
  property: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  texture: ~        # ResourcePath (not annotated — untested)   # ⚠ never used in official YAML
  isLinear: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `materialIndex` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `property` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `texture` | `ResourcePath` | 🟡 public, not annotated (SharpYaml default — untested) | — | ResourcePath |
| `isLinear` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |

## Referenced types

- [`ResourcePath`](ResourcePath.md)
