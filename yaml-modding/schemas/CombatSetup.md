# YAML Schema — `CombatSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `CombatSetup.cs`

- **Inherits**: `YAMLSetup<CombatSetup>`

## Template (every serializable field)

```yaml
baseSpeedPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
baseMissilesPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
missileRange: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
missileRangeForDrones: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
baseMissileDamage: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
healthLossPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
baseMaxHealth: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `baseSpeedPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `baseMissilesPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `missileRange` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `missileRangeForDrones` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `baseMissileDamage` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `healthLossPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `baseMaxHealth` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |
