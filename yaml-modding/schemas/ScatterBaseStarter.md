# YAML Schema — `ScatterBaseStarter`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `InitialSetup.cs`


## Template (every serializable field)

```yaml
my_scatterbasestarter_key:
  resource: ~        # !resource <key> reference   # ⚠ never used in official YAML
  minRadius: ~        # float   # ⚠ never used in official YAML
  maxRadius: ~        # float   # ⚠ never used in official YAML
  qtyMin: ~        # float   # ⚠ never used in official YAML
  qtyMax: ~        # float   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `resource` | `ResourceType` | ✅ YamlMember (confirmed) | — | !resource <key> reference |
| `minRadius` | `float` | ✅ YamlMember (confirmed) | — | float |
| `maxRadius` | `float` | ✅ YamlMember (confirmed) | — | float |
| `qtyMin` | `float` | ✅ YamlMember (confirmed) | — | float |
| `qtyMax` | `float` | ✅ YamlMember (confirmed) | — | float |

## Referenced types

- [`ResourceType`](ResourceType.md)
