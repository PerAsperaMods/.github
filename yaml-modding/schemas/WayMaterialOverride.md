# YAML Schema — `WayMaterialOverride`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `VisualWay.cs`


## Template (every serializable field)

```yaml
my_waymaterialoverride_key:
  albedo: ~        # ResourcePath (not annotated — untested)   # ⚠ never used in official YAML
  normal: ~        # ResourcePath (not annotated — untested)   # ⚠ never used in official YAML
  mask: ~        # ResourcePath (not annotated — untested)   # ⚠ never used in official YAML
  segment: ~        # ResourcePath (not annotated — untested)   # ⚠ never used in official YAML
  color: ~        # hex color (e.g. 8B4513) or {r,g,b,a} map (not annotated — untested)   # ⚠ never used in official YAML
  emissionColor: ~        # hex color (e.g. 8B4513) or {r,g,b,a} map (not annotated — untested)   # ⚠ never used in official YAML
  colorMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
  emissionColorMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
  tiling: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `albedo` | `ResourcePath` | 🟡 public, not annotated (SharpYaml default — untested) | — | ResourcePath |
| `normal` | `ResourcePath` | 🟡 public, not annotated (SharpYaml default — untested) | — | ResourcePath |
| `mask` | `ResourcePath` | 🟡 public, not annotated (SharpYaml default — untested) | — | ResourcePath |
| `segment` | `ResourcePath` | 🟡 public, not annotated (SharpYaml default — untested) | — | ResourcePath |
| `color` | `Color` | 🟡 public, not annotated (SharpYaml default — untested) | — | hex color (e.g. 8B4513) or {r,g,b,a} map |
| `emissionColor` | `Color` | 🟡 public, not annotated (SharpYaml default — untested) | — | hex color (e.g. 8B4513) or {r,g,b,a} map |
| `colorMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `emissionColorMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `tiling` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |

## Referenced types

- [`ResourcePath`](ResourcePath.md)
