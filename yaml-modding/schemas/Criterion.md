# YAML Schema — `Criterion`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `Criterion.cs`

- **Inherits**: `IEquatable<Criterion>`

## Template (every serializable field)

```yaml
my_criterion_key:
  field: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  operation: ~        # enum Operation (Exists, Equals, LesserOrEqual, GreaterOrEqual, InRange, RoundEquals, Lesser, Greater) (not annotated — untested)   # ⚠ never used in official YAML
  value1: ~        # Value (not annotated — untested)   # ⚠ never used in official YAML
  value2: ~        # Value (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `field` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `operation` | `Operation` | 🟡 public, not annotated (SharpYaml default — untested) | — | enum Operation (Exists, Equals, LesserOrEqual, GreaterOrEqual, InRange, RoundEquals, Lesser, Greater) |
| `value1` | `Value` | 🟡 public, not annotated (SharpYaml default — untested) | — | Value |
| `value2` | `Value` | 🟡 public, not annotated (SharpYaml default — untested) | — | Value |

## Enum `Operation`

- `Exists`
- `Equals`
- `LesserOrEqual`
- `GreaterOrEqual`
- `InRange`
- `RoundEquals`
- `Lesser`
- `Greater`

## Constants (known vanilla keys)

- `RE_S` = `\\s*`
- `RE_FIELD` = `\\s*(\\$[a-zA-Z0-9_.]+)\\s*`
- `RE_BIN_OP` = `\\s*(#=|==|<=|>=|<|>)\\s*`
- `RE_TOKEN` = `\\s*(\\$[\\w.]+|\".*\"|[0-9.]+|true|false)\\s*`
- `RE_VALUE` = `\\s*(\".*\"|[0-9.]+|true|false)\\s*`

## Referenced types

- [`Operation`](Operation.md)
