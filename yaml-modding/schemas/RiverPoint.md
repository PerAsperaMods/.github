# YAML Schema — `RiverPoint`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `River.cs`


## Template (every serializable field)

```yaml
my_riverpoint_key:
  x: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
  y: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
  f: ~        # double (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `x` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `y` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `f` | `double` | 🟡 public, not annotated (SharpYaml default — untested) | — | double |
