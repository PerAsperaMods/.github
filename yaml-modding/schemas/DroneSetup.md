# YAML Schema — `DroneSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `DroneSetup.cs`

- **Inherits**: `YAMLSetup<DroneSetup>`

## Template (every serializable field)

```yaml
decayEnabled: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
baseDurationDays: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
moveDurationDays: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
workDurationDays: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
wayWorkDurationDays: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
defaultDrone: ~        # !drone <key> reference (not annotated — untested)   # ⚠ never used in official YAML
aquaticDrone: ~        # !drone <key> reference (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `decayEnabled` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
| `baseDurationDays` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `moveDurationDays` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `workDurationDays` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `wayWorkDurationDays` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `defaultDrone` | `DroneType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !drone <key> reference |
| `aquaticDrone` | `DroneType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !drone <key> reference |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |

## Referenced types

- [`DroneType`](DroneType.md)
