# YAML Schema — `TerraformingProjectSettingType`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `TerraformingProjectSettingType.cs`

- **YAML tag**: `!terraforming_project_setting`
- **manifest.yaml section**: `terraformingProjectSettings:`
- **Inherits**: `StaticDataCollectionItem<TerraformingProjectSettingType>`

## Template (every serializable field)

```yaml
my_terraformingprojectsetting_key:
  isPlaceholder: ~        # bool   # ⚠ never used in official YAML
  projectKey: ~        # string (not annotated — untested)
  position: ~        # int (not annotated — untested)
  positionInCategory: ~        # int (not annotated — untested)
  stage: ~        # int (not annotated — untested)
  backgroundIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') (not annotated — untested)
  pendingLaunchIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') (not annotated — untested)
  completedLaunchIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') (not annotated — untested)
  radius: ~        # float (not annotated — untested)
  iconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') (not annotated — untested)
  categoryType: ~        # TerraformingPlanCategoryType (not annotated — untested)
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `isPlaceholder` | `bool` | ✅ YamlMember (confirmed) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `projectKey` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | string |
| `position` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | int |
| `positionInCategory` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | int |
| `stage` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | int |
| `backgroundIconName` | `Sprite` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `pendingLaunchIconName` | `Sprite` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `completedLaunchIconName` | `Sprite` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `radius` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | float |
| `iconName` | `Sprite` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `categoryType` | `TerraformingPlanCategoryType` | 🟡 public, not annotated (SharpYaml default — untested) | 1× | TerraformingPlanCategoryType |
| `table` | `Dictionary<string, T>` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `StaticValues` | `Dictionary<string, T>.ValueCollection` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `key` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `rootPath` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `resolved` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `isMock` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |

## Referenced types

- [`TerraformingPlanCategoryType`](TerraformingPlanCategoryType.md)
