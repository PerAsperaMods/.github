# YAML Schema — `InitialSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `InitialSetup.cs`

- **Inherits**: `YAMLSetup<InitialSetup>`

## Template (every serializable field)

```yaml
baseGameSpeed: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
building: ~        # !building <key> reference (not annotated — untested)   # ⚠ never used in official YAML
longitude: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
latitude: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
clearAreaRadius: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
manualStartingLocation: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
initializeAdditionalResourceVeins: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
resources: ~        # YAML map; !resource <key> reference (not annotated — untested)   # ⚠ never used in official YAML
baseStarterVeins: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
universeFalseBooleans: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
universeBlackboardZeroNumbers: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
unlockedSpecialProjects: ~        # YAML list; !project <key> reference (not annotated — untested)   # ⚠ never used in official YAML
unlockedKnowledge: ~        # YAML list; !knowledge <key> reference (not annotated — untested)   # ⚠ never used in official YAML
unlockedBuildings: ~        # YAML list; !building <key> reference (not annotated — untested)   # ⚠ never used in official YAML
terraformingQuests: ~        # YAML list; !quest <key> reference (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `baseGameSpeed` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `building` | `BuildingType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !building <key> reference |
| `longitude` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `latitude` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `clearAreaRadius` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `manualStartingLocation` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
| `initializeAdditionalResourceVeins` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
| `resources` | `Dictionary<ResourceType, CargoQuantity>` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML map; !resource <key> reference |
| `baseStarterVeins` | `List<ScatterBaseStarter>` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `universeFalseBooleans` | `string[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `universeBlackboardZeroNumbers` | `string[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `unlockedSpecialProjects` | `SpecialProjectType[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list; !project <key> reference |
| `unlockedKnowledge` | `KnowledgeType[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list; !knowledge <key> reference |
| `unlockedBuildings` | `BuildingType[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list; !building <key> reference |
| `terraformingQuests` | `QuestType[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list; !quest <key> reference |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |

## Referenced types

- [`BuildingType`](BuildingType.md)
- [`CargoQuantity`](CargoQuantity.md)
- [`KnowledgeType`](KnowledgeType.md)
- [`QuestType`](QuestType.md)
- [`ResourceType`](ResourceType.md)
- [`ScatterBaseStarter`](ScatterBaseStarter.md)
- [`SpecialProjectType`](SpecialProjectType.md)
