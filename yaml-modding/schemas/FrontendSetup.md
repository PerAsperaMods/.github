# YAML Schema — `FrontendSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `FrontendSetup.cs`

- **Inherits**: `YAMLSetup<FrontendSetup>`

## Template (every serializable field)

```yaml
invalidPlacementDescriptions: ~        # YAML map; enum Reason (TooFarFromBase, UnevenTerrain, Collision, NeedsResource, IncompatibleResource, BlocksResource, NoSectorPermission, Underwater…) (not annotated — untested)   # ⚠ never used in official YAML
terraformingWidgetsByQuest: ~        # YAML map; !quest <key> reference (not annotated — untested)   # ⚠ never used in official YAML
terraformingCompletedWidgets: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `invalidPlacementDescriptions` | `Dictionary<Planet.InvalidPlacement.Reason, InvalidPlacementDescription>` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML map; enum Reason (TooFarFromBase, UnevenTerrain, Collision, NeedsResource, IncompatibleResource, BlocksResource, NoSectorPermission, Underwater…) |
| `terraformingWidgetsByQuest` | `Dictionary<QuestType, string[]>` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML map; !quest <key> reference |
| `terraformingCompletedWidgets` | `string[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |

## Referenced types

- [`InvalidPlacementDescription`](InvalidPlacementDescription.md)
- [`QuestType`](QuestType.md)
- [`Reason`](Reason.md)
