# YAML Schema — `MaintenanceSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `MaintenanceSetup.cs`

- **Inherits**: `YAMLSetup<MaintenanceSetup>`

## Template (every serializable field)

```yaml
repairDistance: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
repairDamageSelfRatio: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
repairOtherPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
droneMaxHealth: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
healthFactorGoodEnough: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
baseSpeedPerDay: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
buildingDroneCapacity: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `repairDistance` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `repairDamageSelfRatio` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `repairOtherPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `droneMaxHealth` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `healthFactorGoodEnough` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `baseSpeedPerDay` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `buildingDroneCapacity` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |
