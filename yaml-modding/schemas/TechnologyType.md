# YAML Schema — `TechnologyType`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `TechnologyType.cs`

- **YAML tag**: `!technology`
- **manifest.yaml section**: `technology:`
- **Inherits**: `StaticDataCollectionItem<TechnologyType>`

## Template (every serializable field)

```yaml
my_technology_key:
  isPlaceholder: ~        # bool
  IsLane: ~        # bool   # ⚠ never used in official YAML
  name: ~        # string
  shortName: ~        # string
  iconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  popupImageName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  popupSecondaryImageName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  categoryIconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found')
  description: ~        # string
  categoryName: ~        # string
  requiredResearchPoints: ~        # float
  requirements: ~        # YAML list; !technology <key> reference
  actions: ~        # YAML list
  laneColor: ~        # hex color (e.g. 8B4513) or {r,g,b,a} map
  unlockAutomatically: ~        # bool
  position: ~        # int
  connections: ~        # YAML map
  popupOverride: ~        # PopupType
  isRepeatable: ~        # bool   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `isPlaceholder` | `bool` | ✅ YamlMember (confirmed) (inherited from StaticDataCollectionItem) | 92× | bool |
| `IsLane` | `bool` | ✅ YamlMember (confirmed) | 0 ⚠ | bool |
| `name` | `string` | ✅ YamlMember (confirmed) | 96× | string |
| `shortName` | `string` | ✅ YamlMember (confirmed) | 33× | string |
| `iconName` | `Sprite` | ✅ YamlMember (confirmed) | 92× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `popupImageName` | `Sprite` | ✅ YamlMember (confirmed) | 90× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `popupSecondaryImageName` | `Sprite` | ✅ YamlMember (confirmed) | 90× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `categoryIconName` | `Sprite` | ✅ YamlMember (confirmed) | 5× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `description` | `string` | ✅ YamlMember (confirmed) | 95× | string |
| `categoryName` | `string` | ✅ YamlMember (confirmed) | 4× | string |
| `requiredResearchPoints` | `float` | ✅ YamlMember (confirmed) | 96× | float |
| `requirements` | `List<TechnologyType>` | ✅ YamlMember (confirmed) | 96× | YAML list; !technology <key> reference |
| `actions` | `List<TextAction>` | ✅ YamlMember (confirmed) | 109× | YAML list |
| `laneColor` | `Color` | ✅ YamlMember (confirmed) | 5× | hex color (e.g. 8B4513) or {r,g,b,a} map |
| `unlockAutomatically` | `bool` | ✅ YamlMember (confirmed) | 5× | bool |
| `position` | `int` | ✅ YamlMember (confirmed) | 71× | int |
| `connections` | `Dictionary<string, List<Vector2>>` | ✅ YamlMember (confirmed) | 93× | YAML map |
| `popupOverride` | `PopupType` | ✅ YamlMember (confirmed) | 7× | PopupType |
| `isRepeatable` | `bool` | ✅ YamlMember (confirmed) | 0 ⚠ | bool |
| `table` | `Dictionary<string, T>` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `StaticValues` | `Dictionary<string, T>.ValueCollection` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `key` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `rootPath` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `resolved` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `isMock` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |

## ⚠ Fields present in official YAML but absent from current C#

Either renamed via `YamlMember(Name=…)` (name string lives in the binary, not readable from stubs — e.g. `cubeMaterialName` → `cubeMaterial`), or **obsolete fields silently ignored by the loader** (the vanilla files themselves carry dead fields).

- `isLane` (96×)
- `repeatable` (4×)

## Enum `ResearchState`

- `Unresearchable`
- `Researchable`
- `Researched`

## Referenced types

- [`PopupType`](PopupType.md)
- [`TextAction`](TextAction.md)
