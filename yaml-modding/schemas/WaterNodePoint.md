# YAML Schema — `WaterNodePoint`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `WaySetup.cs`


## Template (every serializable field)

```yaml
my_waternodepoint_key:
  lng: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
  lat: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `lng` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `lat` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
