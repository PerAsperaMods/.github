# YAML Schema — `WaySetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `WaySetup.cs`

- **Inherits**: `YAMLSetup<WaySetup>`

## Template (every serializable field)

```yaml
defaultWayType: ~        # !way <key> reference (not annotated — untested)   # ⚠ never used in official YAML
farmsWayType: ~        # !way <key> reference (not annotated — untested)   # ⚠ never used in official YAML
aquaticWayType: ~        # !way <key> reference (not annotated — untested)   # ⚠ never used in official YAML
portuarialWayType: ~        # !way <key> reference (not annotated — untested)   # ⚠ never used in official YAML
superWayMaxDistance: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
superWayMinDistance: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
hyperloopDepth: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
hyperloopSegmentLength: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
heuristicAdditionalFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
heuristicAdditionalFactorRival: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
playerGridResolution: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
rivalGridResolution: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
useTimeQuotaForWayGen: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
useDynamicTimeQuotaForWayGen: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
dynamicTimeQuotaMinFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
dynamicTimeQuotaTargetFps: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
playerTimeQuota: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
rivalTimeQuota: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
playerStepQuota: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
rivalStepQuota: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
aquaticHeuristicAdditionalFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
aquaticGridResolutionMultiplier: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
aquaticMinDepth: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
aquaticShallowWaterFactor: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterNodesCount: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
additionalWaterNodes: ~        # YAML list (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `defaultWayType` | `WayType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !way <key> reference |
| `farmsWayType` | `WayType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !way <key> reference |
| `aquaticWayType` | `WayType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !way <key> reference |
| `portuarialWayType` | `WayType` | 🟡 public, not annotated (SharpYaml default — untested) | — | !way <key> reference |
| `superWayMaxDistance` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `superWayMinDistance` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `hyperloopDepth` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `hyperloopSegmentLength` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `heuristicAdditionalFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `heuristicAdditionalFactorRival` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `playerGridResolution` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `rivalGridResolution` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `useTimeQuotaForWayGen` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
| `useDynamicTimeQuotaForWayGen` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | — | bool |
| `dynamicTimeQuotaMinFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `dynamicTimeQuotaTargetFps` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `playerTimeQuota` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `rivalTimeQuota` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `playerStepQuota` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `rivalStepQuota` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `aquaticHeuristicAdditionalFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `aquaticGridResolutionMultiplier` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `aquaticMinDepth` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `aquaticShallowWaterFactor` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterNodesCount` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `additionalWaterNodes` | `List<WaterNodePoint>` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |

## Referenced types

- [`WaterNodePoint`](WaterNodePoint.md)
- [`WayType`](WayType.md)
