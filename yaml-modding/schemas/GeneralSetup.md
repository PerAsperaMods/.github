# YAML Schema — `GeneralSetup`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `GeneralSetup.cs`

- **Inherits**: `YAMLSetup<GeneralSetup>`

## Template (every serializable field)

```yaml
buildingRequirementSliceSize: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
mastermindCooldownDays: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
waterSamples: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
maxPlacementTries: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
maxChainPlacementTries: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
minJumpDistanceBetweenBuildings: ~        # float (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `buildingRequirementSliceSize` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `mastermindCooldownDays` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `waterSamples` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `maxPlacementTries` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `maxChainPlacementTries` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `minJumpDistanceBetweenBuildings` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | — | float |
| `instance` | `T` | ⚙️ runtime only (not serialized) (inherited from YAMLSetup) | — | T |
