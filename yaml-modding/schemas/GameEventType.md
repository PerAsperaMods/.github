# YAML Schema — `GameEventType`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `GameEventType.cs`

- **Inherits**: `IEquatable<GameEventType>`

## Template (every serializable field)

```yaml
my_gameevent_key:
  eventUid: ~        # int (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `eventUid` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | — | int |
| `Invalid` | `GameEventType` | ⚙️ runtime only (not serialized) | — | GameEventType |
| `_def` | `GameEventTypeDef` | ⚙️ runtime only (not serialized) | — | GameEventTypeDef |

## Enum `ModeHint`

- `IMMEDIATE`
- `DEFERRED`
- `ANY`
- `FIXME_RECHECK`
