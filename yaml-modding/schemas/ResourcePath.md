# YAML Schema — `ResourcePath`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `ResourcePath.cs`


## Template (every serializable field)

```yaml
my_resourcepath_key:
  root: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  path: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `root` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `path` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `IsValid` | `bool` | ⚙️ runtime only (not serialized) | — | bool |
