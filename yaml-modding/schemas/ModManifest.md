# YAML Schema — `ModManifest`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `ModManifest.cs`


## Template (every serializable field)

```yaml
modId: ~        # string (not annotated — untested)
compatibleGameVersions: ~        # YAML list (not annotated — untested)
requiredMods: ~        # YAML list (not annotated — untested)
optionalManifests: ~        # YAML list (not annotated — untested)
generalSetup: ~        # string (not annotated — untested)
initialSetup: ~        # string (not annotated — untested)
droneSetup: ~        # string (not annotated — untested)
scatterSetup: ~        # string (not annotated — untested)
frontendSetup: ~        # string (not annotated — untested)
planetSetup: ~        # string (not annotated — untested)
combatSetup: ~        # string (not annotated — untested)
waySetup: ~        # string (not annotated — untested)
maintenanceSetup: ~        # string (not annotated — untested)
person: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
poi: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
knowledge: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
resource: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
way: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
building: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
buildingCategory: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
terraformingPlanCategory: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
technology: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
site: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
project: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
quest: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
rule: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
behavior: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
perspective: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
transition: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
randomEvent: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
hazardAsteroid: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
hazardDevil: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
hazardSandstorm: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
enhancements: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
river: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
lake: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
tooltip: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
popup: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
aiplayer: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
terraformingGraphSettings: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
terraformingProjectSettings: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
languages: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
drone: ~        # { filenames: [file.yaml, ...], replace: false } (not annotated — untested)
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `modId` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `compatibleGameVersions` | `string[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `requiredMods` | `string[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `optionalManifests` | `string[]` | 🟡 public, not annotated (SharpYaml default — untested) | — | YAML list |
| `generalSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `initialSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `droneSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `scatterSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `frontendSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `planetSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `combatSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `waySetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `maintenanceSetup` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `person` | `ModFileList<Person>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `poi` | `ModFileList<PointOfInterest>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `knowledge` | `ModFileList<KnowledgeType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `resource` | `ModFileList<ResourceType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `way` | `ModFileList<WayType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `building` | `ModFileList<BuildingType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `buildingCategory` | `ModFileList<BuildingCategoryType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `terraformingPlanCategory` | `ModFileList<TerraformingPlanCategoryType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `technology` | `ModFileList<TechnologyType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `site` | `ModFileList<SpecialSite>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `project` | `ModFileList<SpecialProjectType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `quest` | `ModFileList<QuestType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `rule` | `ModFileList<InteractionRule>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `behavior` | `ModFileList<BehaviorTreeDefinition>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `perspective` | `ModFileList<Perspective>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `transition` | `ModFileList<TransitionTable>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `randomEvent` | `ModFileList<RandomEvent>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `hazardAsteroid` | `ModFileList<HazardAsteroidType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `hazardDevil` | `ModFileList<HazardDevilType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `hazardSandstorm` | `ModFileList<HazardSandstormType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `enhancements` | `ModFileList<EnhancementType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `river` | `ModFileList<River>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `lake` | `ModFileList<Lake>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `tooltip` | `ModFileList<TooltipInfo>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `popup` | `ModFileList<PopupType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `aiplayer` | `ModFileList<AIPlayerType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `terraformingGraphSettings` | `ModFileList<TerraformingGraphSettingType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `terraformingProjectSettings` | `ModFileList<TerraformingProjectSettingType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `languages` | `ModFileList<LanguageType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |
| `drone` | `ModFileList<DroneType>` | 🟡 public, not annotated (SharpYaml default — untested) | — | { filenames: [file.yaml, ...], replace: false } |

## Referenced types

- [`AIPlayerType`](AIPlayerType.md)
- [`BehaviorTreeDefinition`](BehaviorTreeDefinition.md)
- [`BuildingCategoryType`](BuildingCategoryType.md)
- [`BuildingType`](BuildingType.md)
- [`DroneType`](DroneType.md)
- [`EnhancementType`](EnhancementType.md)
- [`HazardAsteroidType`](HazardAsteroidType.md)
- [`HazardDevilType`](HazardDevilType.md)
- [`HazardSandstormType`](HazardSandstormType.md)
- [`InteractionRule`](InteractionRule.md)
- [`KnowledgeType`](KnowledgeType.md)
- [`Lake`](Lake.md)
- [`LanguageType`](LanguageType.md)
- [`ModFileList`](ModFileList.md)
- [`Person`](Person.md)
- [`Perspective`](Perspective.md)
- [`PointOfInterest`](PointOfInterest.md)
- [`PopupType`](PopupType.md)
- [`QuestType`](QuestType.md)
- [`RandomEvent`](RandomEvent.md)
- [`ResourceType`](ResourceType.md)
- [`River`](River.md)
- [`SpecialProjectType`](SpecialProjectType.md)
- [`SpecialSite`](SpecialSite.md)
- [`TechnologyType`](TechnologyType.md)
- [`TerraformingGraphSettingType`](TerraformingGraphSettingType.md)
- [`TerraformingPlanCategoryType`](TerraformingPlanCategoryType.md)
- [`TerraformingProjectSettingType`](TerraformingProjectSettingType.md)
- [`TooltipInfo`](TooltipInfo.md)
- [`TransitionTable`](TransitionTable.md)
- [`WayType`](WayType.md)
