# YAML Schema — `InvalidPlacementDescription`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `FrontendSetup.cs`


## Template (every serializable field)

```yaml
my_invalidplacementdescription_key:
  name: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
  iconName: ~        # string (not annotated — untested)   # ⚠ never used in official YAML
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `name` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `iconName` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | — | string |
| `DisplayName` | `string` | ⚙️ runtime only (not serialized) | — | string |
