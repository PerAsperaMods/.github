# YAML Schema — `TerraformingGraphSettingType`

> Generated by `Tools/extract_yaml_schemas.py` from the IL2CPP dump — do not edit by hand.
> Source: `TerraformingGraphSettingType.cs`

- **YAML tag**: `!terraforming_graph_setting`
- **manifest.yaml section**: `terraformingGraphSettings:`
- **Inherits**: `StaticDataCollectionItem<TerraformingGraphSettingType>`

## Template (every serializable field)

```yaml
my_terraforminggraphsetting_key:
  isPlaceholder: ~        # bool   # ⚠ never used in official YAML
  dataKey: ~        # string (not annotated — untested)
  factionData: ~        # bool (not annotated — untested)   # ⚠ never used in official YAML
  criterionKey: ~        # string (not annotated — untested)
  min: ~        # float (not annotated — untested)
  max: ~        # float (not annotated — untested)
  position: ~        # int (not annotated — untested)
  positionInCategory: ~        # int (not annotated — untested)
  general: ~        # bool (not annotated — untested)
  color: ~        # hex color (e.g. 8B4513) or {r,g,b,a} map (not annotated — untested)
  lineColor: ~        # hex color (e.g. 8B4513) or {r,g,b,a} map (not annotated — untested)
  iconName: ~        # sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') (not annotated — untested)
  unit: ~        # string (not annotated — untested)
  valueStringOffset: ~        # float (not annotated — untested)
  derived: ~        # bool (not annotated — untested)
  deriveFromDataA: ~        # string (not annotated — untested)
  deriveFromDataB: ~        # string (not annotated — untested)
  usewarnings: ~        # bool (not annotated — untested)
  warningValue: ~        # float (not annotated — untested)
  dangerValue: ~        # float (not annotated — untested)
  categoryType: ~        # TerraformingPlanCategoryType (not annotated — untested)
  dataAsPercentage: ~        # bool (not annotated — untested)
  tooltipKeyOverride: ~        # string (not annotated — untested)
```

## All members

| Field | C# type | YAML status | Vanilla usage | Notes |
|-------|---------|-------------|---------------|-------|
| `isPlaceholder` | `bool` | ✅ YamlMember (confirmed) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `dataKey` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | string |
| `factionData` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | 0 ⚠ | bool |
| `criterionKey` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | string |
| `min` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | float |
| `max` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | float |
| `position` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | int |
| `positionInCategory` | `int` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | int |
| `general` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | bool |
| `color` | `Color` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | hex color (e.g. 8B4513) or {r,g,b,a} map |
| `lineColor` | `Color` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | hex color (e.g. 8B4513) or {r,g,b,a} map |
| `iconName` | `Sprite` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | sprite path (string, resolved at load — if missing: log 'Sprite at path … couldn't be found') |
| `unit` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | string |
| `valueStringOffset` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | float |
| `derived` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | bool |
| `deriveFromDataA` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | string |
| `deriveFromDataB` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | string |
| `usewarnings` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | bool |
| `warningValue` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | float |
| `dangerValue` | `float` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | float |
| `categoryType` | `TerraformingPlanCategoryType` | 🟡 public, not annotated (SharpYaml default — untested) | 11× | TerraformingPlanCategoryType |
| `dataAsPercentage` | `bool` | 🟡 public, not annotated (SharpYaml default — untested) | 3× | bool |
| `tooltipKeyOverride` | `string` | 🟡 public, not annotated (SharpYaml default — untested) | 3× | string |
| `table` | `Dictionary<string, T>` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `StaticValues` | `Dictionary<string, T>.ValueCollection` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | YAML map |
| `key` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `rootPath` | `string` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | string |
| `resolved` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |
| `isMock` | `bool` | ⚙️ runtime only (not serialized) (inherited from StaticDataCollectionItem) | 0 ⚠ | bool |

## Referenced types

- [`TerraformingPlanCategoryType`](TerraformingPlanCategoryType.md)
